import React, { FC } from 'react';

const Comment: FC = () => {
  return (
    <div
      style={{
        backgroundColor: 'black',
        color: 'white',
        width: '100%',
        marginTop: '1rem',
      }}
    >
      new comment
    </div>
  );
};

export default Comment;
